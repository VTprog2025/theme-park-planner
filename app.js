document.addEventListener('DOMContentLoaded', () => {

    // TODO: Check which page we are on and initialize accordingly

    // TODO: If on index.php, attach filter button event listener and load rides

    // TODO: If on itinerary.php, load the user's itinerary

});

// TODO: loadRides() - build query params from filters and fetch get_rides.php
function loadRides() {

}

// TODO: renderRides(rides) - loop through rides array and build HTML cards
function renderRides(rides) {

}

// TODO: addToItinerary(rideId) - POST to add_itinerary.php with user_id and ride_id
function addToItinerary(rideId) {

}

// TODO: loadItinerary() - fetch get_itinerary.php with user_id
function loadItinerary() {

}

// TODO: renderItinerary(rides) - loop through rides and build itinerary cards
function renderItinerary(rides) {

}

// TODO: removeFromItinerary(rideId) - POST to remove_itinerary.php then reload itinerary
function removeFromItinerary(rideId) {

}
